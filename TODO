!. 분기, 반복문에 주석
!. 전부 다 POST로 넘기기
!. ajax 로 저장 후 해당 리스트 페이지로 리다이렉트 
!. logback cron 확인
!. JSP 파일은 WEB-INF 아래
!. 람다 x 
!. 파일 저장 경로 properties로 관리
!. 페이징 리펙토링 (페이징 html 모델로 그려서 전달, 공통부분 코드 유틸로 분리)
!. 검색기능 구현 (날짜설정, 제목 내용)
!. 첨부파일 다운로드 구조 변경 (일자//폴더uuid//파일(중복처리)) -> 첨부파일 다운로드 시 이름만
!. 조인으로 데이터 가져오기
!. 파일 다중첨부(+, -)
!. 예외처리(page번호, 게시글 제목내용 빈칸,  html 삽입...)